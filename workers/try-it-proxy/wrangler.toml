name = "cicd-try-it-proxy"
main = "src/index.js"
compatibility_date = "2024-12-01"

# Create KV: npx wrangler kv namespace create RATE_LIMITS
# Then paste the id below before deploying.
[[kv_namespaces]]
binding = "RATE_LIMITS"
id = "REPLACE_WITH_KV_NAMESPACE_ID"

[vars]
GITHUB_OWNER = "njvanas"
GITHUB_REPO = "CI-CD"
GITHUB_REF = "master"
GITHUB_WORKFLOW = "try-it.yml"
ALLOWED_ORIGINS = "https://njvanas.github.io,http://127.0.0.1:8765,http://localhost:8765"

# Secret (never commit):
#   npx wrangler secret put GITHUB_TOKEN
